{% extends 'base.html.twig' %}

{# {% form_theme form 'bootstrap_base_layout.html.twig' %} #}

{% block title %}Modifier le Produit{% endblock %}

{% block body %}
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Modifier le Produit {{ product.nom }} {{ product.conditionnement }} {{ product.unit }}</h3>
                <div class="card-tools">
                        {{ include('product/_delete_form.html.twig') }}    
                </div>            
            </div>
            <!-- /.card-header -->
            <div class="card-body">

                {{ include('product/_form.html.twig', {'button_label': 'Valider'}) }}
                <a href="{{ path('product_index') }}"><i class="fas fa-angle-double-left"></i></a>
            </div>
        </div>
    </div>


{% endblock %}
